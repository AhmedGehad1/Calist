openpyxl>=3.1
xlrd>=2.0

# Pinned below 6.0: the interface is built and tested against the 5.2 API, and
# 6.x is a major release that has not been verified here.
customtkinter>=5.2,<6

# Optional — enables dragging a folder onto the window. Without it the drop
# zone is click-only and everything else works unchanged.
# tkinterdnd2>=0.3

# Development only:
#   pip install pytest

openpyxl>=3.1
xlrd>=2.0

# 6.0 verified screen by screen against 5.2.2 (tools/snap_ui.py): a one-pixel
# shift of right-aligned controls, nothing else. Below 7: a major release would
# need the same check again.
customtkinter>=6.0,<7

# Optional — enables dragging a folder onto the window. Without it the drop
# zone is click-only and everything else works unchanged.
# tkinterdnd2>=0.3

# Development only:
#   pip install pytest

openpyxl>=3.1
xlrd>=2.0

# 6.0 verified screen by screen against 5.2.2 (tools/snap_ui.py): a one-pixel
# shift of right-aligned controls, nothing else. Below 7: a major release would
# need the same check again.
customtkinter>=6.0,<7

# Dragging a folder or files onto the window. MIT licensed; it loads a native
# tkdnd DLL, bundled by calist.spec. Should it be missing or fail to load, the
# window opens click-only and everything else works unchanged.
tkinterdnd2>=0.6.3,<0.7

# Development only:
#   pip install pytest
